# syntax=docker/dockerfile:1.7

# Dependency image: node_modules only.
# Tagged by the hash of package.json + package-lock.json, rebuilt only when they change.
FROM node:24-slim

WORKDIR /app

COPY package.json package-lock.json ./

RUN --mount=type=cache,target=/root/.npm \
	npm ci --no-audit --no-fund
